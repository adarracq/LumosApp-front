export default class Theme {
    title: string;
    description: string;
    idea: string;

    constructor(title: string, description: string, idea: string) {
        this.title = title;
        this.description = description;
        this.idea = idea;
    }
}